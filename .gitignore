# Build results
result
result-*
# Hardware config (auto-generated per machine)
host/hardware-configuration.nix

# Certificates (never commit!)
certs/
*.crt
*.key
*.p12

# Swap and temp files
*.swp
*~
.direnv/
