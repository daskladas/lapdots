# Build results
result
result-*
# Hardware configs (wird auf Laptop neu generiert!)
host/hardware-configuration.nix

# Certificates (NIE committen!)
certs/
*.crt
*.key
*.p12

# Swap und temp
*.swp
*~
.direnv/
